node_modules/
dist/
# Built API bundle & DB are committed so Vercel can pick them up.
# (Locally rebuilt by 'npm run build' + script/build-api.ts.)
# api/index.js
# api/index.cjs
# api/data.db
.env
.env.local
.DS_Store
*.log
.vite/

# SQLite WAL/SHM sidecar files — keep data.db committed but not the transient runtime files
data.db-shm
data.db-wal

# Editor
.vscode/
.idea/

# User uploads
uploads/
.vercel
