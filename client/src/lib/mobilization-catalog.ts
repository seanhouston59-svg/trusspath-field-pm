// Convenience re-export so client code can import from "@/lib/mobilization-catalog"
// alongside its other lib imports. The catalog itself lives in shared/ because
// the server imports it for seeding.
export * from "@shared/mobilization-catalog";
